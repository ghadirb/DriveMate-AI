name: one-shot navigation regression repair (manual only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "One-shot repair completed; this workflow is now disabled. Android Build is the repository health check."
